---
title: SteBre’s Portfolio
layout: base.njk
templateClass: portfolio
eleventyNavigation:
  key: "Portfolio"
  order: "1"
sitemapPriority: 1.0
---
<header class="header containerL">
  <h1 class="title">SteBre’s Portfolio</h1>
</header>

<section class="filter containerL">
	<h2 class="sectionTitle">Nach einem Schlagwort filtern</h2>
	<ul class="tagList">
		{% for tag in collections.projects | getAllTags | sortTagList | excludeTagsFromTagList %}
		{% set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
			<li><a href="{{ tagUrl }}" class="post-tag">{{ tag }}</a></li>
		{% endfor %}
	</ul>
</section>

<section class="results containerL">
  <div class="grid">
		{% set results = collections.projects | reverse %}
		{% include "partials/preview.njk" %}
	</div>
</section>